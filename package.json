{
  "name": "gt-thumb-gen",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf docs/* dist .cache",
    "build": "parcel build index.html --out-dir docs --public-url https://segeek.github.io/gt-thumb-gen/",
    "dev": "parcel index.html"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^1.9.7",
    "rimraf": "^2.6.2",
    "typescript": "^3.5.1"
  }
}
